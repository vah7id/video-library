export const selectStyles = {
    control: (base: any) => ({
        ...base,
        minHeight: '42px',
        padding: '0',
        boxShadow: 'none'
    }),
    input: (base: any) => ({
        ...base,
        margin: '0',
        padding: '0',
        boxShadow: 'none'
    }),
    valueContainer: (base: any) => ({
        ...base,
        padding: '8px 12px',
        margin: '0'
    })
};